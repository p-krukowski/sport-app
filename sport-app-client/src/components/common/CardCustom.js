import styled from "styled-components";
import {theme} from "../../util/theme";

const CardCustom = styled.div`
  background: ${theme.colors.navbar};
  border: solid 1px ${theme.colors.navbar};
  border-radius: 5px;
  padding: 5px;
`

export default CardCustom;
